# Base URL for the backend API (used only for YouTube search, never for the local music library).
# In development, Vite proxies /api to the server (see vite.config.js), so this can stay as /api.
# In production, point this to your deployed backend, e.g. https://api.yourdomain.com/api
VITE_API_BASE_URL=/api
